{
    "extends": [
        "config:best-practices",
        ":disableDependencyDashboard",
        ":automergePr",
        ":automergeMinor",
        ":semanticCommitTypeAll(chore)",
        ":label(dependencies)"
    ],
    "addLabels": [
        "{{category}}",
        "{{datasource}}",
        "{{manager}}"
    ]
}
